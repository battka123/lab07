name: Docker

on:
  push:
    branches: 
      main 

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build
      # создаю билд и запускаю докер
        run: |
          sudo docker build -t myapp ./hello_world
          sudo docker run myapp
